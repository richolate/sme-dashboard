# Copy this file to drizzle/.env (or point DRIZZLE_ENV to your main .env)
# Use the same credentials you already configured for Django.

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=sme_dashboard
# Or provide a full connection string instead
# DATABASE_URL=postgresql://postgres:your_password@localhost:5432/sme_dashboard
